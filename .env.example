# Copy this to .env and adjust as needed.
PORT=8010
# (Add future secrets/config here)
APP_NAME=Diet App
APP_VERSION=0.2.0
LOG_LEVEL=INFO
CORS_ORIGINS=*

DATABASE_URL=sqlite:///data/app.db

# Postgres example (uncomment and edit):
# DATABASE_URL=postgresql+psycopg://dietapp:REPLACE_ME@127.0.0.1:5432/dietapp

SECRET_KEY=change-me
ACCESS_TOKEN_EXPIRE_MINUTES=60


# Auth
# Generate a strong value and place it in .env (do NOT commit real secrets)
# openssl rand -base64 32 | tr '+/' '-_'
SECRET_KEY=REPLACE_ME
ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_ALGORITHM=HS256
